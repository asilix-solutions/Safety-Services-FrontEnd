# Stripe test-mode publishable key (safe for the browser). Get it from
# https://dashboard.stripe.com/test/apikeys — never put a secret key (sk_test_/sk_live_) here.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here
