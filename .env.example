# Base URL for the backend API. The MVP runs on LocalStorage mocks and does not
# require this to be set; the axios client falls back to its own default.
NEXT_PUBLIC_API_URL=https://api.safety-licensing.com/v1
